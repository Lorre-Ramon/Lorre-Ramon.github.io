# TODO
Last Updated: 2026-09-09

## Immediate
- [ ] Re-authenticate GitHub: `gh auth login` (token for `Lorre-Ramon` is expired)
- [ ] Create the repo `Lorre-Ramon.github.io` and push `main`
- [ ] In repo Settings → Pages, set source to **Deploy from a branch → main → / (root)**
- [ ] Verify the live site: HTTPS, root-absolute asset paths, `404.html`, `#terminal` deep link
- [ ] Replace the template entry in `content/shelf.json` with real reading notes

## Upcoming
- [ ] Write `PROJECT.md` (human-only file; Claude is not to author it)
- [ ] Decide whether to keep `boxuans@andrew.cmu.edu` anywhere public — it expires after Dec 2026
- [ ] Consider a custom domain (needs `CNAME` + DNS)
- [ ] Regenerate résumé PDFs whenever content JSON changes — they do not auto-update

## Blocked
- [ ] Push to GitHub — blocked on: expired `gh` token; user must re-auth or create the repo manually
